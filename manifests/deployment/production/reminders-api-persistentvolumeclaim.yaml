apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: reminders-api-claim
  name: reminders-api-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
